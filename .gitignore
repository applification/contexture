node_modules
dist
out
.DS_Store
*.log
package-lock.json

coverage/
.turbo
.next
next-env.d.ts
*.tsbuildinfo
storybook-static/
test-results/
playwright-report/
apps/desktop/build/bin/

# Environment files. Real values are resolved via varlock + Infisical; the
# committed `.env.schema` files declare structure only. Any local `.env*`
# files are escape hatches and stay out of git.
.env
.env.local
.env.*.local
.sandcastle/.env
!.env.schema
!**/.env.schema

# varlock typegen output
**/env.d.ts

# Paperclip agent worktrees and local state
.paperclip/

# Codex Desktop local state
.codex/*
!.codex/agents/
!.codex/agents/*.toml
!.codex/skills/
!.codex/skills/**

# Claude Code scheduled-task runtime artefacts
.claude/scheduled_tasks.lock

# Claude Code session-local permissions (auto-created on launch, including
# inside sandcastle's bind-mounted worktree).
.claude/settings.local.json

# Sensitive docs migrated to Paperclip issue documents (ONT-49)
Marketing/

# Scratch files for testing
scratch/
