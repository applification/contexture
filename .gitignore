node_modules
dist
out
.DS_Store
*.log
package-lock.json

coverage/
.turbo
.next
next-env.d.ts
*.tsbuildinfo
storybook-static/
test-results/
playwright-report/

# Environment files
.env
.env.local
.env.*.local
.sandcastle/.env

# Paperclip agent worktrees and local state
.paperclip/

# Claude Code scheduled-task runtime artefacts
.claude/scheduled_tasks.lock

# Claude Code session-local permissions (auto-created on launch, including
# inside sandcastle's bind-mounted worktree).
.claude/settings.local.json

# Sensitive docs migrated to Paperclip issue documents (ONT-49)
Marketing/
