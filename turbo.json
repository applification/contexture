{
  "$schema": "https://v2-9-7.turborepo.dev/schema.json",
  "globalEnv": ["INFISICAL_CLIENT_ID", "INFISICAL_CLIENT_SECRET"],
  "globalDependencies": ["**/.env.schema"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**", "dist/**", ".next/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
